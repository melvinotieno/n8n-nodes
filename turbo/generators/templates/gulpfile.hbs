import path from "node:path";
import { dest, src, task } from "gulp";

task("build:icons", copyIcons);

function copyIcons() {
	const source = path.resolve("src", "**", "*.{png,svg}");
	const destination = path.resolve("dist");

	return src(source).pipe(dest(destination));
}
