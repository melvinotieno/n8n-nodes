export class {{ pascalCase name }} implements ICredentialType {
	displayName = "{{ titleCase name }}";

	name = "{{ camelCase name }}";

	documentationUrl = `https://github.com/melvinotieno/n8n-nodes/tree/main/packages/{{ pascalCase name }}/README.md#{{ dashCase name }}-credentials`;
	
	properties: INodeProperties[] = [];
}
